% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betaStability.R
\name{betaStability}
\alias{betaStability}
\title{Beta Stability Calculation with Multiple Prediction Methods}
\usage{
betaStability(
  comtable = NULL,
  envmeta = NULL,
  comdist = NULL,
  envdist = NULL,
  sitenames = NULL,
  method = "linearPred",
  X = NULL,
  Y = NULL,
  geo_enabled = TRUE,
  GAM.dist.method = "manhattan",
  vegdist.method = "bray",
  seed = 42
)
}
\arguments{
\item{comtable}{The community table (required).}

\item{envmeta}{The environmental metadata table/matrix (required).}

\item{comdist}{The community dissimilarity matrix (optional). If not
provided, computed from comtable using vegdist().}

\item{envdist}{The environmental dissimilarity matrix (optional). If not
provided, computed from envmeta using euclidean distance on range-normalized
envmeta.}

\item{sitenames}{The names of the site (optional, default: NULL)}

\item{method}{A character string or vector specifying the prediction
method(s) to use. Available options: "linearPred", "mlPred", "glmPred",
"gamPred", "gdmPred", "rfPred", "xgboostPred". Use "all" to run all methods.
Default is "linearPred".}

\item{X}{The X coordinates of the sites for gdmPred (optional, default: NULL)}

\item{Y}{The Y coordinates of the sites for gdmPred (optional, default: NULL)}

\item{geo_enabled}{Whether to include geographic info for gdmPred (default: TRUE)}

\item{GAM.dist.method}{The method for calculating dist for gamPred (default: "manhattan")}

\item{vegdist.method}{The method for calculating comdist from comtable (default: "bray")}

\item{seed}{The random seed for reproducibility of rfPred and xgboostPred (default: 42)}
}
\value{
If method length is 1, returns a column vector of predicted stability values.
If method length > 1, returns a data frame with each column representing results
from each selected method.
}
\description{
This function integrates various prediction methods (linear, multiple linear,
generalized linear model, generalized additive model, generalized
dissimilarity model, random forest, and xgboost) to calculate the stability
of each site.
}
\examples{
data(varespec)
data(varechem)

# Single method (linearPred)
result_linear <- betaStability(comtable = varespec,
                               envmeta = varechem,
                               method = "linearPred")

# Multiple methods
results_multi <- betaStability(comtable = varespec,
                               envmeta = varechem,
                               method = c("linearPred", "mlPred"))

}
